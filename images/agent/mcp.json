{
  "mcpServers": {
    "message-queue": {
      "command": "node",
      "args": ["/opt/mcp/message-queue-server.js"],
      "env": { "REDIS_URL": "${REDIS_URL}", "THREAD_ID": "${THREAD_ID}" }
    }
  }
}
